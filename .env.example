# Required: Postgres connection string
DATABASE_URL=postgresql://user:password@localhost:5432/bulkcoach

# Required: Session secret for Express (any random string)
SESSION_SECRET=change-me-to-a-random-string

# Expo (set automatically on Replit, only needed for local dev)
# EXPO_PUBLIC_DOMAIN=localhost:5000
